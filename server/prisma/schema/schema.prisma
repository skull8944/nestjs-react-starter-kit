generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters", "views", "relationJoins"]
  binaryTargets   = ["native"]
}

datasource db {
  provider = "postgresql"
  url      = env("dbConnectionString")
  schemas  = ["public", "permission", "job"]
}
