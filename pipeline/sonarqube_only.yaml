sonarqube-tests:
  stage: sonarqube
  allow_failure: true
  trigger:
    include:
      - local: pipeline/lib/sonarqube_scan.yaml
    strategy: depend
  only:
    refs:
      - sonarqube
      - chore/SonarQube